# agents.md

本文件用于约束 AI 在本仓库中改造回测框架时的工作流程。目标是把现有日线级回测逐步升级为“无限逼近真实交易”的中国 A 股市场事件驱动回测系统：在任意时刻只允许使用该时刻及之前已经真实发生、已经可获得的数据，并按真实时间流逐根 K 线回放。

## 1. 总目标

- 市场范围：中国 A 股（CN market）。
- 数据范围：本地分钟数据、日线数据、交易日历、涨跌停状态、停牌状态、复权/不复权价格、手续费配置等。
- 回测原则：模拟真实数据流与真实交易时间流，而不是在完整历史数据表上一次性计算未来可见结果。
- 扩展能力：支持多标的、多线程/多进程回测、参数遍历、Walk-Forward、结果汇总与可复现审计。

## 2. 绝对禁止事项

AI 修改回测相关代码时，必须主动检查并避免以下问题：

- 禁止未来函数：任何策略、因子、筛选、风控、调参、撮合逻辑都不得读取当前事件时间之后的数据。
- 禁止用全量历史一次性计算后再切片给策略使用，除非明确证明每个时间点只依赖历史窗口。
- 禁止在 t1 收盘时使用 t2 的开盘、分钟线、涨跌停、停牌、成交结果或任何未来标记。
- 禁止用测试集结果反向选择参数、标的、阈值或规则。
- 禁止在参数遍历中共享可变账户、持仓、订单、随机状态或缓存对象。
- 禁止为了跑通测试而弱化真实交易约束。

## 3. 时间流语义

任何新代码都必须明确事件时间和数据可见性：

- t1 收盘后：只能访问 t1 及之前的日线、分钟线、指标、账户、订单、成交、风控状态。
- t2 盘前：只能访问 t1 收盘后可获得的信息和 t2 盘前已知的交易日历、停牌/涨跌停规则配置；不得访问 t2 盘中或收盘数据。
- t2 交易时段：按分钟 K 线逐根推进。处理完 09:31 的事件后，策略最多只能看到 09:31 及之前的数据。
- t2 收盘后：才能把 t2 全日数据纳入日线聚合、盘后指标、盘后信号和次日计划。

推荐把所有回测流程抽象为事件序列：

1. 盘前事件：加载今日可交易状态、昨夜订单、风控开关。
2. 开盘事件：处理集合竞价/开盘价相关撮合。
3. 分钟事件：逐根分钟 K 线推进，更新账户、撮合、策略、风控。
4. 收盘事件：生成当日收盘后可见数据、日线聚合、盘后信号。
5. 结算事件：记录资金、持仓、订单、成交、费用、绩效快照。

## 4. 中国市场交易约束

实现或修改撮合、订单、账户、风控时，应优先补齐以下真实交易约束：

- T+1：当日买入股票原则上当日不可卖出，必须按持仓批次记录可卖数量。
- 涨跌停：涨停通常不可买入成交，跌停通常不可卖出成交；不得只用收盘价判断。
- 停牌：停牌时不得撮合，不得产生虚假价格。
- 交易单位：A 股买入通常按 100 股整数倍；卖出需符合实际可卖数量规则。
- 手续费：佣金、印花税、过户费、最低佣金、滑点、冲击成本应集中配置并可测试。
- 订单生命周期：至少区分当日有效、撤单、未成交、部分成交、拒单。
- 价格规则：限价单、市价近似、开盘价、最高/最低触达、分钟内高低点撮合逻辑必须显式说明。
- 成交保守性：分钟 K 线无法还原逐笔顺序时，必须采用保守撮合假设，并在文档和测试中说明。

## 5. 架构改造方向

AI 应尽量按小步重构推进，不要一次性重写整个框架。建议拆分为以下模块：

- `DataPortal`：按时间点提供“当前可见数据”，屏蔽未来数据。
- `TradingCalendar`：管理交易日、开收盘时间、午休、节假日。
- `EventClock` / `ReplayEngine`：生成盘前、开盘、分钟、收盘、结算事件。
- `BrokerSimulator`：负责订单校验、撮合、费用、成交、拒单、撤单。
- `Account` / `Portfolio`：管理现金、冻结资金、持仓批次、可卖数量、市值和净值。
- `StrategyContext`：策略唯一入口，只暴露当前时刻允许访问的数据和交易接口。
- `BacktestEngine`：编排事件流、策略、撮合、账户、风控和记录器。
- `ParameterRunner`：执行参数遍历、多进程任务分发、结果收集和异常隔离。
- `ResultStore`：保存成交、订单、分钟级净值、日级净值、参数、配置哈希和运行元数据。

## 6. 参数遍历与并行回测约束

- 每个参数组合必须拥有独立的策略实例、账户实例、撮合器实例和随机种子。
- 并行任务不得写同一个结果文件；应先写独立目录或临时文件，再统一汇总。
- 参数组合、数据版本、代码版本、配置版本必须可追踪。
- 多线程/多进程只用于不同标的、不同参数组合或不同 Walk-Forward 窗口之间的隔离任务。
- 如果底层计算依赖 pandas/numpy 大对象，应优先只读共享数据，禁止在子任务中原地修改共享 DataFrame。

## 7. AI 修改代码流程

每次处理回测框架相关任务时，AI 必须遵守：

1. 先阅读现有文档、回测相关源代码目录和相关测试，理解当前假设。
2. 明确本次改动处于哪个层级：数据可见性、事件时钟、撮合、账户、策略接口、并行参数遍历、结果记录或测试。
3. 先写或更新能暴露未来函数、撮合错误、T+1、涨跌停、停牌、参数隔离问题的测试。
4. 再做最小代码改动，不要同时混入无关重构。
5. 所有新增接口必须说明事件时间、数据可见性和状态归属。
6. 改动后运行现有测试；若测试失败，区分本次引入问题和历史遗留问题。
7. 在最终说明中列出：改动范围、真实交易约束覆盖情况、仍未覆盖的假设。

## 8. 测试要求

新增或修改回测能力时，至少考虑以下测试：

- t1 收盘信号不能访问 t2 数据。
- 分钟回放中，当前分钟不能访问后续分钟。
- 日线指标滚动窗口只依赖当前及历史数据。
- 涨停买入、跌停卖出、停牌撮合均被正确拒绝或跳过。
- T+1 可卖数量正确变化。
- 手续费、印花税、过户费、滑点计算可复现。
- 同一参数组合单进程与并行执行结果一致。
- 不同参数组合之间无账户、订单、持仓状态污染。
- Walk-Forward 中训练、验证、测试窗口严格隔离。

## 9. 当前仓库上下文

当前仓库已有日级事件驱动回测雏形：

- `src/backtest/engine.py`：单标的日级回测引擎，当前按“昨日盘后挂单、今日撮合、今日收盘后生成次日订单”的顺序运行。
- `run_backtest.py`：负责标的筛选、验证段选 K、测试段评估和组合汇总。
- `tests/`：已有 OU 估计、网格引擎、风控、回测引擎测试。

后续 AI 不应直接推翻这些模块，而应以兼容方式逐步引入分钟级事件流、真实撮合、账户批次和并行参数遍历。

## 10. 交付标准

一个回测框架改动只有在满足以下条件时才可认为完成：

- 数据可见性边界清晰，无法通过公共接口读取未来数据。
- 交易事件顺序清晰，撮合和策略调用顺序可审计。
- 中国市场核心约束至少被测试覆盖或明确列为未实现假设。
- 参数遍历结果可复现，失败任务不影响其他任务。
- 输出结果包含配置、参数、数据区间、交易日志、订单日志、成交日志和净值曲线。
