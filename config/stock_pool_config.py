# ====================================================================
# 标的池配置
# 对应 README 阶段 1.1 项目结构中的 config/stock_pool_config.py
#
# 作用：
#   1. 集中存放 Tushare token。
#   2. 定义候选标的池（README 建议：沪深300 + 中证500 成分股）。
#
# 说明：
#   为了让回测「开箱即用」、保持简洁，这里内置了一小批
#   流动性好、较具代表性的蓝筹/白马股作为默认候选池。
#   你可以：
#     - 直接修改 DEFAULT_STOCK_POOL 列表增删标的；
#     - 或把 USE_INDEX_COMPONENTS 设为 True，
#       由 fetcher 通过 Tushare 拉取沪深300/中证500 的真实成分股。
# ====================================================================

# -------------------- Tushare Token --------------------
# Tushare Pro 的 API token。用于调用行情接口。
# 注意：真实项目中应通过环境变量或私密配置注入，避免明文写入源码。
#       此处按任务要求内置，方便直接运行回测。
TUSHARE_TOKEN = "d7351dd4c51b1ed397084bac62c57c7a8bb4248e464ca99716d290c6"

# -------------------- 候选标的池来源开关 --------------------
# False：使用下方 DEFAULT_STOCK_POOL 内置列表（默认，离线友好、运行快）。
# True ：调用 Tushare 拉取沪深300 + 中证500 真实成分股（约 800 只，耗时较长）。
USE_INDEX_COMPONENTS = False

# 当 USE_INDEX_COMPONENTS = True 时使用的指数代码。
INDEX_CODES = [
    "000300.SH",   # 沪深300
    "000905.SH",   # 中证500
]

# -------------------- 默认候选标的池 --------------------
# 代码格式遵循 Tushare 规范：6 位代码 + 交易所后缀（.SH 上海 / .SZ 深圳）。
# 这里选取的是不同行业、流动性充足的代表性标的，作为开箱即用的演示池。
DEFAULT_STOCK_POOL = [
    "600519.SH",  # 贵州茅台（白酒）
    "601318.SH",  # 中国平安（保险）
    "600036.SH",  # 招商银行（银行）
    "000858.SZ",  # 五粮液（白酒）
    "600276.SH",  # 恒瑞医药（医药）
    "601166.SH",  # 兴业银行（银行）
    "000333.SZ",  # 美的集团（家电）
    "600900.SH",  # 长江电力（电力，低波动）
    "601012.SH",  # 隆基绿能（光伏）
    "600030.SH",  # 中信证券（券商）
    "000651.SZ",  # 格力电器（家电）
    "601888.SH",  # 中国中免（免税）
    "002415.SZ",  # 海康威视（安防）
    "600887.SH",  # 伊利股份（食品）
    "601398.SH",  # 工商银行（银行，低波动）
]


def get_stock_pool():
    """返回候选标的池列表。

    供 screener / fetcher 调用。当前直接返回内置列表；
    如需使用真实指数成分股，可在 fetcher 中根据
    USE_INDEX_COMPONENTS 开关改为在线拉取。

    Returns:
        list[str]: Tushare 格式的股票代码列表。
    """
    return list(DEFAULT_STOCK_POOL)
